const accents2 = "áéíóúÁÉÍOÚ";
const vowels2 = "aeiouAEIOU";

function formatText(text) {
    text.toLowerCase(text);
    text.replaceAll(" ", "");

    return text;
}

function removeAccents(text) {
    
}

function stringDivide(text) {
    for (let i = 0; i < text.length / 2; i++ ) {

    }

}


function isPalindrome(text) {

    

}

//-----------------------------------------------------------

function calculator (op1, op2, opName, fn) {
    console.log(`The result of ${op1} ${opName} ${op2} is ${fn (op1,op2)}`);

}

function suma (a, b) {
    return a+b;
}

calculator(7, 14, "sumado a", suma)


//-------------------------------------------------------------
//ejercicio arrays

let numeros = 

function myArray() {
    for (let i = 0; 0 < i < 100; i++) {

    }
}